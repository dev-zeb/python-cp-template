from setuptools import setup

setup(
    name="python_cp_template",
    version="0.1",
    py_modules=["python_cp_template"],  # This points to the python_cp_template.py file
    install_requires=[],                # We can add dependencies here if needed
)
